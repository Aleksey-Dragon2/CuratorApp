<Window x:Class="CuratorApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин:" Grid.Row="0"/>
        <TextBox x:Name="UsernameBox" Grid.Row="1" Margin="0,5,0,10"/>

        <TextBlock Text="Пароль:" Grid.Row="2"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,5,0,10"/>

        <Button Grid.Row="4" Content="Войти" Height="30" Click="Login_Click" HorizontalAlignment="Right" Width="100" Margin="0,10,0,0"/>
    </Grid>
</Window>
